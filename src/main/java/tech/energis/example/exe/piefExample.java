/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package tech.energis.example.exe;

import java.util.Collection;
import tech.energis.example.entity.Organization;
import tech.energis.example.entity.OrganizationFinder;
import tech.energis.example.entity.OrganizationImplList;

/**
 *
 * @author pokvalitov
 */
public class piefExample
{
    /*
    public static void main(String[] args)
    {
        OrganizationFinder finder = new OrganizationImplList();
        Collection<Organization> organizations = finder.findAll();
        organizations.forEach( 
                org -> System.out.println( String.format( "%S: %s (%s)",
                        org.getTaxId(), org.getShortName(), org.getFullName()
                        ))
            );
    }
    */
}
